import { provideIcons } from '@ng-icons/core';
import { aspectsTrash, aspectsCheckmark, aspectsEdit, aspectsUndo } from '@ng-icons/ux-aspects';

export const ICONS_PROVIDER = provideIcons({
  aspectsTrash,
  aspectsCheckmark,
  aspectsEdit,
  aspectsUndo,
});
